# =============================================================================
# YAPÓ 3.0 – Variables de entorno
# Copiar a .env o .env.local y ajustar. No commitear .env ni .env.local.
# El mismo código corre en localhost, Vercel y ngrok; todo se decide por estas vars.
# =============================================================================

# -----------------------------------------------------------------------------
# Variables públicas (cliente) – mismo nombre en todos los entornos
# -----------------------------------------------------------------------------
# WebSocket: chat + señalización WebRTC. Backend externo (no incluido en este repo).
# - local:   ws://localhost:3001 (o dejar vacío y el código usa fallback localhost:3001)
# - Vercel:  wss://tu-servidor-ws.com (obligatorio si usás chat/video)
# - ngrok:   wss://xxxx.ngrok.io (túnel a tu backend WS en 3001)
NEXT_PUBLIC_WS_URL=

# Safe Mode: sesión mock sin login; Cerebro no bloquea; biometría saltada.
# - local / ngrok: true para desarrollo
# - Vercel: true en preview, false en producción si tenés login real
NEXT_PUBLIC_SAFE_MODE=true

# Google Maps: mapa geolocalización, marcadores por oficio/empresas/profesionales. Obtener clave en https://console.cloud.google.com/ (Maps JavaScript API).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Cerebro: local = rules + KB; openai = delegar a OpenAI (requiere OPENAI_API_KEY en servidor).
NEXT_PUBLIC_AI_MODE=local

# Voz: local = SpeechSynthesis; elevenlabs = API (requiere ELEVENLABS_API_KEY en servidor).
NEXT_PUBLIC_VOICE_MODE=local

# -----------------------------------------------------------------------------
# Servidor (no exponer al cliente)
# -----------------------------------------------------------------------------
# Auth.js (NextAuth v5) – obligatorio para login real. Dev: devsecret123
AUTH_SECRET="devsecret123"

# OAuth (crear apps en cada proveedor y configurar callback URL: https://tu-dominio/api/auth/callback/<provider>)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_FACEBOOK_ID=
AUTH_FACEBOOK_SECRET=
AUTH_INSTAGRAM_ID=
AUTH_INSTAGRAM_SECRET=

# PostgreSQL – Prisma. Obligatorio para auth real (login, perfil, consentimientos).
# - local: postgresql://usuario:password@localhost:5432/yapo
# - Vercel: usar DATABASE_URL del add-on o variable de proyecto
DATABASE_URL="postgresql://postgres:hellskitchen999@localhost:5432/yapo"

# Wallet API: URL del backend de billetera. Proxy vía /api/wallet.
# - local/ngrok: http://localhost:3002
# - Vercel: URL del backend desplegado
WALLET_API_URL=

# Opcional: cliente llama directo al backend. Vacío = mismo origen (/api/wallet).
NEXT_PUBLIC_WALLET_API_URL=

# WhatsApp soporte YAPÓ (código país + número sin +). Ej: 595981555555
NEXT_PUBLIC_YAPO_WHATSAPP=595981555555
YAPO_WHATSAPP_NUMBER=595981555555

# Safe Mode (servidor): YAPO_SAFE_MODE o SAFE_MODE=true para login mock.
YAPO_SAFE_MODE=true
SAFE_MODE=true

# Rol del usuario safe (vale, capeto, kavaju, mbarete, cliente, pyme, enterprise).
# NEXT_PUBLIC_SAFE_ROLE=capeto
# YAPO_SAFE_ROLE=capeto

# Master Key (solo desarrollo): si definís una clave, login con esa clave devuelve sesión master.
# YAPO_MASTER_KEY=

# Cerebro / Voz – API keys SOLO en servidor (usadas en /api/ai y /api/voice).
# OPENAI_API_KEY=
# ELEVENLABS_API_KEY=

# -----------------------------------------------------------------------------
# Entorno
# -----------------------------------------------------------------------------
NODE_ENV=development

# -----------------------------------------------------------------------------
# QA / Testing – Ver docs/QA-TEST-PLAN.md
# localhost: NEXT_PUBLIC_WS_URL vacío → fallback ws://localhost:3001
# Vercel:   NEXT_PUBLIC_WS_URL=wss://tu-servidor-ws.com (obligatorio chat/video)
# ngrok:    ngrok http 3001 → NEXT_PUBLIC_WS_URL=wss://xxxx.ngrok.io
# -----------------------------------------------------------------------------
