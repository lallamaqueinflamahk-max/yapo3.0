/**
 * Componentes de validación biométrica.
 * UI separada de lógica (lib/auth/biometric) y provider (IBiometricProvider).
 */

export { default as BiometricValidationModal } from "./BiometricValidationModal";
export type {
  BiometricValidationModalProps,
  BiometricModalState,
} from "./BiometricValidationModal";
